import React from 'react';
import './Homepage.scss';
import MenuContainer from '../../component/menu-container-directory/MenuContainer';

export const Homepage = () => (
    <div className="homepage">
        <MenuContainer />
         
    </div>
);